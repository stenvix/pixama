<Page
    x:Class="Pixama.App.Views.LayoutView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pixama.App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:Pixama.Logic.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <NavigationView x:Name="Navigation" PaneDisplayMode="Top" IsBackButtonVisible="Collapsed" IsSettingsVisible="False">
        <NavigationView.MenuItemTemplate>
            <DataTemplate x:DataType="viewModels:LayoutMenuItemViewModel">
                <NavigationViewItem Content="{Binding Name}"/>
            </DataTemplate>
        </NavigationView.MenuItemTemplate>
        <Frame x:Name="MainFrame"></Frame>
    </NavigationView>
</Page>
